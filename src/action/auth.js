import { REGISTER_SUCCESS, 
    REGISTER_FAIL, 
    LOGIN_SUCCESS, 
    LOGIN_FAIL, 
    LOAD_USER, 
    AUTH_ERROR
} from '../constant/constant'

export const loadUser = () => async dispatch => {
    try {

    } catch(error){

    }
}